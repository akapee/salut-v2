name: Deploy Next.js to Production

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install latest npm
        run: npm install -g npm@latest

      - name: Install Dependencies
        run: npm install

      - name: Build Next.js
        run: npm run build
        env:
          # Variabel .env produksi
          NEXT_PUBLIC_API_URL: ${{ secrets.API_PRODUCTION_URL }}

      - name: Prepare Standalone Package
        run: |
          mkdir -p out_standalone
          cp -r .next/standalone/* out_standalone/
          cp -r .next/static out_standalone/.next/
          cp -r public out_standalone/

      - name: FTP Deploy to cPanel Production
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_PRODUCTION_USER }}
          password: ${{ secrets.FTP_PRODUCTION_PASSWORD }}
          local-dir: ./out_standalone/
          server-dir: /home/salutper/public_html/
